local M = {}

-- function M.devcontainer(op, args)
-- 	if string.find(args, 'workspace%-folder') then
-- 		print(vim.fn.system(config.devcontainer.path .. op .. ' ' .. args))
-- 	else
-- 		print(vim.fn.system(config.devcontainer.path .. ' ' .. op .. ' --workspace-folder=. ' .. args))
-- 	end
-- end

return M
